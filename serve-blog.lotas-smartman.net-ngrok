docker run --rm -v "$(pwd):/src" -w /src ruby:2.3 sh -c 'bundle install --path vendor/bundle && exec jekyll build -s blog.lotas-smartman.net/ -d blog.lotas-smartman.net/_site/'
docker run --name tiernanotoolecom -v "$(pwd)/blog.lotas-smartman.net/_site/:/usr/share/nginx/html:ro" -d nginx
docker run --rm -it --link tiernanotoolecom wernight/ngrok ngrok http tiernanotoolecom:80
