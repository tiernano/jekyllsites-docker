docker run --rm -v "$(pwd):/src" -w /src ruby:2.3 sh -c 'bundle install --path vendor/bundle && exec jekyll build -s blog.lotas-smartman.net/ -d blog.lotas-smartman.net/_site/'
docker run --name bloglotas-smartmannet -v "$(pwd)/blog.lotas-smartman.net/_site/:/usr/share/nginx/html:ro" -d nginx
docker run --rm -it --link bloglotas-smartmannet wernight/ngrok ngrok http bloglotas-smartmannet:80
